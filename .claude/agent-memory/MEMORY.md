# MEMORY index — trillium-starter

Created 2026-09-21 during the Windows-to-Ubuntu migration (no prior memory
existed in the repo or the Windows backup).

- Per-student Codespaces template (school account `ryannorris-trillium`).
- Python pinned to 3.12 in `.devcontainer/` (pygame has no 3.14 wheels) — don't bump.
- Related context lives in trillium-physics memory:
  `project_cs_demo_codespace.md`, `reference_browser_lua_limits.md`.
- Sibling repo `trillium-starters` holds the extra starters pulled via `get.sh`.
